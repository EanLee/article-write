# Week 2 Day 6 - P0-1 設定介面開發計畫

> **日期**: 2026-02-07 (週五)
> **負責人**: Taylor (CTO)
> **目標**: 完成 P0-1 設定介面 UI
> **預估工時**: 8 小時
> **Checkpoint**: 2026-02-09 EOD

---

## 🎯 今日目標

**主要任務**: 建立可用的設定介面，讓使用者能夠設定並儲存路徑

**驗收標準**：
- [ ] 可透過 UI 選擇 Obsidian Vault 路徑
- [ ] 可透過 UI 選擇 Astro 專案路徑
- [ ] 路徑驗證正確（有效/無效提示）
- [ ] 設定可在 UI 中即時顯示

**不在範圍內**：
- ❌ 設定持久化（明天做）
- ❌ 複雜的驗證邏輯（基本驗證即可）
- ❌ UI 美化（能用就好）
- ❌ 錯誤處理優化（基本錯誤即可）

---

## 📋 詳細任務拆解

### Task 1: 建立 Settings.vue 基礎結構 (1.5 小時)

**目標**: 建立設定頁面的基本架構

**子任務**：
- [ ] 建立 `src/components/Settings.vue` 檔案
- [ ] 設計基本 UI 布局（2 欄式：側邊欄 + 主內容區）
- [ ] 建立 Tab 切換機制（一般設定、Git 設定）
- [ ] 整合到主應用程式路由

**驗收**：
- 可從主選單開啟設定頁面
- 可在不同 Tab 之間切換
- UI 布局清晰可用

**參考**：
- 現有的 `SettingsPanel.vue`（可參考但需重構）
- 使用 Vue 3 Composition API

---

### Task 2: Obsidian Vault 路徑選擇器 (2 小時)

**目標**: 實作 Obsidian Vault 路徑選擇功能

**子任務**：
- [ ] 建立路徑輸入框（顯示當前路徑）
- [ ] 建立「瀏覽」按鈕（使用 Electron dialog）
- [ ] 實作 IPC 通訊（renderer → main）
- [ ] 實作路徑驗證邏輯（檢查目錄是否存在）
- [ ] 顯示驗證結果（✓ 有效 / ✗ 無效）

**IPC Handler** (Main Process):
```typescript
// src/main/handlers/config.ts
ipcMain.handle('config:selectObsidianPath', async () => {
  const result = await dialog.showOpenDialog({
    properties: ['openDirectory'],
    title: '選擇 Obsidian Vault 目錄'
  })
  
  if (result.canceled) return null
  
  return result.filePaths[0]
})

ipcMain.handle('config:validatePath', async (_, path: string) => {
  try {
    const stats = await fs.promises.stat(path)
    return stats.isDirectory()
  } catch {
    return false
  }
})
```

**驗收**：
- 點擊「瀏覽」可開啟檔案選擇器
- 選擇的路徑會顯示在輸入框
- 路徑驗證正確（有效/無效）
- 無效路徑顯示錯誤訊息

---

### Task 3: Astro 專案路徑選擇器 (1.5 小時)

**目標**: 實作 Astro 專案路徑選擇功能

**子任務**：
- [ ] 建立路徑輸入框（顯示當前路徑）
- [ ] 建立「瀏覽」按鈕（使用 Electron dialog）
- [ ] 複用 Task 2 的 IPC 通訊
- [ ] 實作路徑驗證邏輯（檢查目錄是否存在）
- [ ] 顯示驗證結果（✓ 有效 / ✗ 無效）

**驗收**：
- 點擊「瀏覽」可開啟檔案選擇器
- 選擇的路徑會顯示在輸入框
- 路徑驗證正確（有效/無效）
- 無效路徑顯示錯誤訊息

---

### Task 4: 路徑驗證邏輯強化 (1.5 小時)

**目標**: 實作更完善的路徑驗證

**子任務**：
- [ ] **Obsidian Vault 驗證**：
  - [ ] 檢查目錄是否存在
  - [ ] 檢查是否為有效的 Obsidian Vault（包含 `.obsidian` 資料夾）
  - [ ] 顯示清楚的錯誤訊息
  
- [ ] **Astro 專案驗證**：
  - [ ] 檢查目錄是否存在
  - [ ] 檢查是否為有效的 Astro 專案（包含 `astro.config.*`）
  - [ ] 顯示清楚的錯誤訊息

**驗證邏輯** (Main Process):
```typescript
// src/main/services/PathValidator.ts
export async function validateObsidianVault(path: string): Promise<ValidationResult> {
  try {
    // 1. 檢查目錄是否存在
    const dirExists = await fs.promises.stat(path)
    if (!dirExists.isDirectory()) {
      return {
        valid: false,
        error: '所選路徑不是有效的目錄'
      }
    }
    
    // 2. 檢查是否為 Obsidian Vault
    const obsidianDir = path + '/.obsidian'
    const obsidianExists = await fs.promises.access(obsidianDir)
      .then(() => true)
      .catch(() => false)
    
    if (!obsidianExists) {
      return {
        valid: false,
        error: '所選目錄不是有效的 Obsidian Vault（缺少 .obsidian 資料夾）'
      }
    }
    
    return { valid: true }
  } catch (error) {
    return {
      valid: false,
      error: `驗證失敗: ${error.message}`
    }
  }
}

export async function validateAstroProject(path: string): Promise<ValidationResult> {
  try {
    // 1. 檢查目錄是否存在
    const dirExists = await fs.promises.stat(path)
    if (!dirExists.isDirectory()) {
      return {
        valid: false,
        error: '所選路徑不是有效的目錄'
      }
    }
    
    // 2. 檢查是否為 Astro 專案
    const configFiles = ['astro.config.js', 'astro.config.ts', 'astro.config.mjs']
    let configExists = false
    
    for (const file of configFiles) {
      const filePath = path + '/' + file
      const exists = await fs.promises.access(filePath)
        .then(() => true)
        .catch(() => false)
      if (exists) {
        configExists = true
        break
      }
    }
    
    if (!configExists) {
      return {
        valid: false,
        error: '所選目錄不是有效的 Astro 專案（找不到 astro.config.*）'
      }
    }
    
    return { valid: true }
  } catch (error) {
    return {
      valid: false,
      error: `驗證失敗: ${error.message}`
    }
  }
}
```

**驗收**：
- Obsidian Vault 驗證正確（有 .obsidian 資料夾）
- Astro 專案驗證正確（有 astro.config.*）
- 錯誤訊息清楚明確
- UI 即時顯示驗證結果

---

### Task 5: 設定狀態管理 (1 小時)

**目標**: 在 Vue 中管理設定狀態

**子任務**：
- [ ] 建立 `useSettings` Composable
- [ ] 管理 Obsidian Vault 路徑狀態
- [ ] 管理 Astro 專案路徑狀態
- [ ] 管理驗證狀態（loading, valid, error）
- [ ] 實作即時驗證（路徑變更時自動驗證）

**Composable 實作**:
```typescript
// src/composables/useSettings.ts
import { ref, watch } from 'vue'

export function useSettings() {
  const obsidianPath = ref<string>('')
  const astroPath = ref<string>('')
  
  const obsidianValidation = ref<{
    loading: boolean
    valid: boolean
    error?: string
  }>({
    loading: false,
    valid: false
  })
  
  const astroValidation = ref<{
    loading: boolean
    valid: boolean
    error?: string
  }>({
    loading: false,
    valid: false
  })
  
  // 選擇 Obsidian 路徑
  async function selectObsidianPath() {
    const path = await window.api.config.selectObsidianPath()
    if (path) {
      obsidianPath.value = path
      await validateObsidianPath()
    }
  }
  
  // 驗證 Obsidian 路徑
  async function validateObsidianPath() {
    if (!obsidianPath.value) return
    
    obsidianValidation.value.loading = true
    try {
      const result = await window.api.config.validateObsidianPath(obsidianPath.value)
      obsidianValidation.value = {
        loading: false,
        valid: result.valid,
        error: result.error
      }
    } catch (error) {
      obsidianValidation.value = {
        loading: false,
        valid: false,
        error: error.message
      }
    }
  }
  
  // 類似的實作給 Astro 路徑...
  
  return {
    obsidianPath,
    astroPath,
    obsidianValidation,
    astroValidation,
    selectObsidianPath,
    validateObsidianPath,
    // ...
  }
}
```

**驗收**：
- 路徑狀態正確管理
- 驗證狀態正確管理
- 路徑變更時自動驗證
- Loading 狀態正確顯示

---

### Task 6: UI 整合與測試 (0.5 小時)

**目標**: 整合所有功能並進行測試

**子任務**：
- [ ] 整合所有組件
- [ ] 手動測試完整流程
- [ ] 修復發現的問題
- [ ] 確認 UI 可用性

**測試場景**：
1. 開啟設定頁面
2. 選擇有效的 Obsidian Vault
3. 確認顯示「✓ 有效」
4. 選擇無效的目錄
5. 確認顯示錯誤訊息
6. 選擇有效的 Astro 專案
7. 確認顯示「✓ 有效」
8. 選擇無效的目錄
9. 確認顯示錯誤訊息

**驗收**：
- 所有測試場景通過
- UI 反應流暢
- 錯誤訊息清楚
- 無明顯 Bug

---

## 📝 開發檢查清單

### 開始前
- [ ] 確認在 `develop` 分支
- [ ] `git pull origin develop` 確保最新
- [ ] 建立 feature 分支：`git checkout -b feature/p0-1-settings-ui`
- [ ] 執行 `pnpm run test` 確認測試通過
- [ ] 執行 `pnpm run dev` 確認應用程式可運行

### 開發中
- [ ] 遵循 TDD：先寫測試，再寫實作
- [ ] 每完成一個 Task，執行測試確認通過
- [ ] Commit 訊息遵循 Conventional Commits
- [ ] 每個 Task 完成後 commit（原子性提交）

### 完成後
- [ ] 所有測試通過 (`pnpm run test`)
- [ ] ESLint 無錯誤 (`pnpm run lint`)
- [ ] TypeScript 編譯通過
- [ ] 手動測試完整流程
- [ ] 推送到遠端：`git push origin feature/p0-1-settings-ui`
- [ ] 更新進度追蹤表

---

## ⚠️ 注意事項

### 必須遵守
1. ✅ **只做 P0-1 相關工作**：不做任何 UI 美化、測試覆蓋率提升等
2. ✅ **能用就好**：不追求完美，確保功能可用即可
3. ✅ **遇到問題立即提出**：不要硬撐，在 Standup 或 GitHub Issue 提出
4. ✅ **保持溝通**：進度更新、阻礙報告要及時

### 絕對禁止
1. ❌ 不做 UI/UX 優化（非必要）
2. ❌ 不追求完美的錯誤處理
3. ❌ 不撰寫非必要的文檔
4. ❌ 不重構非關鍵程式碼
5. ❌ 不做任何「順便做」的事情

---

## 🎯 成功定義

**今日成功 = 可透過 UI 選擇路徑並驗證**

不是：
- ❌ 完美的 UI
- ❌ 100% 的錯誤處理
- ❌ 完整的測試覆蓋

而是：
- ✅ 功能可用
- ✅ 驗證正確
- ✅ 無明顯 Bug

---

## 📊 預期成果

### 預期 Commit
```
feat(settings): 實作設定介面基礎結構

- 建立 Settings.vue 組件
- 實作 Tab 切換機制
- 整合到主應用程式路由

feat(settings): 實作 Obsidian Vault 路徑選擇

- 建立路徑輸入框和瀏覽按鈕
- 實作 IPC 通訊（selectObsidianPath）
- 實作路徑驗證邏輯

feat(settings): 實作 Astro 專案路徑選擇

- 建立路徑輸入框和瀏覽按鈕
- 複用 IPC 通訊機制
- 實作路徑驗證邏輯

feat(settings): 強化路徑驗證邏輯

- 驗證 Obsidian Vault（檢查 .obsidian 資料夾）
- 驗證 Astro 專案（檢查 astro.config.*）
- 顯示清楚的錯誤訊息

feat(settings): 實作設定狀態管理

- 建立 useSettings Composable
- 管理路徑狀態和驗證狀態
- 實作即時驗證
```

### 預期檔案變更
```
新增:
- src/components/Settings.vue
- src/composables/useSettings.ts
- src/main/handlers/config.ts
- src/main/services/PathValidator.ts
- tests/composables/useSettings.test.ts (基本測試)

修改:
- src/main.ts (註冊 IPC handlers)
- src/App.vue (新增設定頁面路由)
```

---

## 📅 時程安排

| 時間 | 任務 | 預估工時 |
|------|------|---------|
| 09:00-10:00 | Task 1: Settings.vue 基礎結構 | 1h |
| 10:00-10:15 | **Daily Standup** | 0.25h |
| 10:15-11:00 | Task 1: Settings.vue 基礎結構（續） | 0.5h |
| 11:00-13:00 | Task 2: Obsidian Vault 路徑選擇器 | 2h |
| 13:00-14:00 | 午休 | - |
| 14:00-15:30 | Task 3: Astro 專案路徑選擇器 | 1.5h |
| 15:30-17:00 | Task 4: 路徑驗證邏輯強化 | 1.5h |
| 17:00-18:00 | Task 5: 設定狀態管理 | 1h |
| 18:00-18:30 | Task 6: UI 整合與測試 | 0.5h |
| 18:30-19:00 | Commit & Push & 更新進度 | 0.5h |

**總計**: 8 小時

---

## 🔗 相關資源

### 文檔
- [緊急圓桌會議記錄](../roundtable-discussions/2026-02-06-emergency-progress-review.md)
- [團隊承諾書](../roundtable-discussions/2026-02-06-team-commitment.md)
- [MVP 範圍定義](./MVP_SCOPE.md)

### 技術參考
- [Electron Dialog API](https://www.electronjs.org/docs/latest/api/dialog)
- [Vue 3 Composition API](https://vuejs.org/guide/extras/composition-api-faq.html)
- [IPC 通訊範例](https://www.electronjs.org/docs/latest/tutorial/ipc)

### 測試
- 現有的 Obsidian Vault：`C:\Repos\obsidian-vault-test`
- 現有的 Astro 專案：`C:\Repos\astro-blog-test`

---

**規劃日期**: 2026-02-06  
**執行日期**: 2026-02-07  
**負責人**: Taylor (CTO)  
**檢查人**: Alex (PM)

**讓我們專注、高效地完成今天的任務！💪**
